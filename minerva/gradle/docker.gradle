docker {
    javaApplication {
        ports = [ 8080 ]
        baseImage = 'eclipse-temurin:17.0.7_7-jre-alpine'
        images = [ findProperty('dockerhub') + project.name ]
        maintainer = 'SoltauFintel'
        mainClassName = application.mainClass
    }
}
